export enum IframeEventTypes
{
        OnBeforeUnload = 0,
        OnLoad = 1,
        OnUnload = 2,
        OnPageShow = 3,
        OnPageHide = 4,
        OnResize = 5,
        OnFormSubmit = 6,
        OnPageError = 999
}
